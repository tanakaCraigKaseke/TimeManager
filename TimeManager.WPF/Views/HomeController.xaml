<UserControl x:Class="TimeManager.WPF.Views.HomeController"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimeManager.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="Welcome to the Time manager app" FontSize="20"></TextBlock>
            <!-- Submit Button -->
           <Button   Content="Login" Width="100" Margin="10" VerticalAlignment="Center" HorizontalAlignment="Center" />
            <!-- Login Button -->
           <Button   Content="Register" Width="100" Margin="10" VerticalAlignment="Center" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</UserControl>
